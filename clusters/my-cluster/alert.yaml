apiVersion: notification.toolkit.fluxcd.io/v1beta1
kind: Alert
metadata:
  generation: 2
  name: rpe-alerts
  namespace: rpe
spec:
  providerRef:
    name: slack
  eventSeverity: info
  eventSources:
    - kind: GitRepository
      namespace: rpe
    - kind: Kustomization
      namespace: rpe
    - kind: HelmRelease
      namespace: rpe
    - kind: HelmChart
      namespace: rpe
    - kind: HelmRepository
      namespace: rpe
    - kind: ImageRepository
      namespace: rpe
    - kind: ImagePolicy
      namespace: rpe
    - kind: ImageUpdateAutomation
      namespace: rpe